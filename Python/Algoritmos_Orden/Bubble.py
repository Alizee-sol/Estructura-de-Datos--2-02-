def bubbleSort(array):
    s = len(array)

    for j in range(s)
    
        swapped = False

        for i = 1 to n-1
            if array[i-1] > array[i], then
            swap(array[i-1], array[i])
